# Codex MCP Offline-First Pack

This patch upgrades the previous architecture pack with strict offline-first requirements.

Use these files to force Codex/Cursor agents to:
- avoid cloud dependency creep
- avoid required external APIs
- keep MCP runtime local-first
- keep embeddings local by default
- preserve performance and privacy
